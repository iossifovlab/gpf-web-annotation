import { KeyValueDisplayPipe } from './key-value-display.pipe';

describe('KeyValueDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new KeyValueDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
